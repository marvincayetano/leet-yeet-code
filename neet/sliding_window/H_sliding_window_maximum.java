class Solution {
  public int[] maxSlidingWindow(int[] nums, int k) {

    // Explanation: We use a deque to keep track of the index of the elements in the
    // current window
    // Monotoic queue - A monotonic Queue is a data structure the elements from the
    // front to the end is strictly either increasing or decreasing.

    // We use a deque to keep track of the index of the elements in the current
    // window
  }
}